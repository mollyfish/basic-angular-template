# Angular with Gulp, Sass and Express Template

Simple project using Angular. Runs on an Express server.  Uses Gulp (3.x.x) and Webpack for preprocessing of Sass.

To run:

1. clone down the repo
2. run npm install
3. run gulp
4. run node server.js
5. open in browser at localhost:3000